[package]
name = "test_schema_store"
edition = "2024"

[dependencies]
reqwest = { version = "0.13.0", default-features = false, features = [
    "blocking",
    "default-tls",
] }

[lib]
crate-type = ["cdylib", "lib"]
